# @framelab/canvas

The Framelab canvas UI — static files served locally by `@framelab/server` (port 3133).

Part of [Framelab](https://www.npmjs.com/package/framelab) — install the `framelab` CLI instead of using this directly.

MIT
